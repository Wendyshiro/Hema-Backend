name: Cervical Cancer Model Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:  # Allows manual trigger from GitHub UI

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        lfs: true  # If you're using Git LFS for large model files
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov pytest-html
        
        # Install project dependencies
        if [ -f requirements.txt ]; then
          pip install -r requirements.txt
        else
          # Install common ML dependencies
          pip install pandas numpy scikit-learn xgboost imbalanced-learn joblib openpyxl
        fi
    
    - name: Verify model files exist
      run: |
        echo "Checking for model files..."
        if [ ! -d "models" ]; then
          echo "⚠️ Warning: models/ directory not found"
          echo "Tests that require models will be skipped"
        else
          echo "✅ models/ directory found"
          ls -lh models/
        fi
    
    - name: Run tests with pytest
      run: |
        # Run tests with coverage
        pytest test_predict_risk.py -v --cov=predict_risk --cov-report=xml --cov-report=term-missing --cov-report=html
      continue-on-error: false
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false
    
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: test-results-${{ matrix.python-version }}
        path: |
          htmlcov/
          coverage.xml
    
    - name: Generate test summary
      if: always()
      run: |
        echo "## Test Results Summary 🧪" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Python Version:** ${{ matrix.python-version }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Add pytest output to summary (if exists)
        if [ -f pytest-report.txt ]; then
          echo "### Test Output" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat pytest-report.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
        fi

  lint:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install linting tools
      run: |
        pip install flake8 black isort
    
    - name: Run flake8
      run: |
        # Stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # Exit-zero treats all errors as warnings
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      continue-on-error: true
    
    - name: Check code formatting with black
      run: |
        black --check predict_risk.py cervical_train_fixed.py || true
      continue-on-error: true

  security:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install safety bandit
    
    - name: Run safety check (dependency vulnerabilities)
      run: |
        pip freeze | safety check --stdin || true
      continue-on-error: true
    
    - name: Run bandit (security linter)
      run: |
        bandit -r . -f json -o bandit-report.json || true
      continue-on-error: true
    
    - name: Upload security reports
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: security-reports
        path: |
          bandit-report.json

  model-validation:
    runs-on: ubuntu-latest
    needs: test  # Run after tests pass
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install pandas numpy scikit-learn xgboost joblib
    
    - name: Validate model files
      run: |
        python -c "
        import joblib
        import os
        
        print('🔍 Validating model files...')
        
        required_files = [
            'models/rf_regressor.pkl',
            'models/ensemble_classifier.pkl',
            'models/scaler.pkl',
            'models/feature_cols.pkl',
            'models/risk_category_map.pkl'
        ]
        
        all_exist = True
        for file in required_files:
            if os.path.exists(file):
                print(f'✅ {file}')
                try:
                    model = joblib.load(file)
                    print(f'   Successfully loaded')
                except Exception as e:
                    print(f'   ⚠️ Error loading: {e}')
                    all_exist = False
            else:
                print(f'❌ {file} - NOT FOUND')
                all_exist = False
        
        if all_exist:
            print('\\n✅ All model files validated successfully!')
        else:
            print('\\n⚠️ Some model files are missing or invalid')
        " || echo "⚠️ Model validation skipped (models not in repo)"